// GENERATE BY ./src/scripts/generateComponents.ts
// DON NOT EDIT IT MANUALLY

import React from "react";

import { iconStata1Inch } from "../../../icons/full/build/icon-stata1inch.icon";
import { IconComponentBaseProps } from "../../../utils";
import { SVG } from "../../Base/SVG";

const Stata1inchIcon = (props: IconComponentBaseProps) => {
  return <SVG svgCode={iconStata1Inch.data} {...props} />;
};

if (process.env.NODE_ENV !== 'production') {
  Stata1inchIcon.displayName = 'Stata1inchIcon';
}

export default Stata1inchIcon;