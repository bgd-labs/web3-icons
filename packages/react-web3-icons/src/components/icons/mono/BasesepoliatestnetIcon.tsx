// GENERATE BY ./src/scripts/generateComponents.ts
// DON NOT EDIT IT MANUALLY

import React from "react";

import { iconBasesepoliatestnet } from "../../../icons/mono/build/icon-basesepoliatestnet.icon";
import { IconComponentBaseProps } from "../../../utils";
import { SVG } from "../../Base/SVG";

const BasesepoliatestnetIcon = (props: IconComponentBaseProps) => {
  return <SVG svgCode={iconBasesepoliatestnet.data} {...props} />;
};

if (process.env.NODE_ENV !== 'production') {
  BasesepoliatestnetIcon.displayName = 'BasesepoliatestnetIcon';
}

export default BasesepoliatestnetIcon;