// GENERATE BY ./src/scripts/generateComponents.ts
// DON NOT EDIT IT MANUALLY

import React from "react";

import { iconAstkgho } from "../../../icons/mono/build/icon-astkgho.icon";
import { IconComponentBaseProps } from "../../../utils";
import { SVG } from "../../Base/SVG";

const AstkghoIcon = (props: IconComponentBaseProps) => {
  return <SVG svgCode={iconAstkgho.data} {...props} />;
};

if (process.env.NODE_ENV !== 'production') {
  AstkghoIcon.displayName = 'AstkghoIcon';
}

export default AstkghoIcon;